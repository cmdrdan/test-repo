---
name: "LiveTV Scheduler"
guid: "f4c6e3a8-b7d1-4e2f-9a5c-8d3b1e6f7a90"
version: "1.0.1.0"
targetAbi: "10.11.6.0"
framework: "net9.0"
overview: "Create virtual live TV channels from your media library"
description: >
  Simulates live TV programming using your existing Jellyfin media library.
  Create custom channels, assign shows and movies, and generate automatic
  schedules. Streams directly from local files with no external dependencies
  or transcoding overhead.
category: "Live TV"
owner: "cmdrdan"
artifacts:
  - "Jellyfin.Plugin.LiveTV.dll"
changelog: >
  Add per-channel Stream Mode setting. On-demand (default) starts each
  tune-in at the beginning of the current program with correct progress
  reporting; Live seeks into the program at the scheduled offset using
  HLS transcoding so it behaves like real broadcast TV.
